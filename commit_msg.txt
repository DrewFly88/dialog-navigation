fix: navigation formula restored to totalCards - parserIdx + dynamic batch count

Key fixes:
- Revert formula: childrenIndex = totalCards - parserIdx (NOT actualSDKCount - parserIdx)
  SDK loads NEWEST cards first: children[1] = SDK card S-1, children[N] = SDK card S-N
  So totalCards - parserIdx correctly indexes into the DOM
- Dynamic batch count: calculate how many loadMore cycles are needed based on
  (childrenIndex - currentBubbles + 1) / PAGE_SIZE, instead of fixed 9 cycles
- Increased batch wait from 600ms to 800ms for SDK render stability

Co-Authored-By: AtomCode (deepseek-v4-flash) <noreply@atomgit.com>
